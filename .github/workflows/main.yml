name: FitnessTrackr Tests

on:
  push:
    branches:
      - main
  pull_request:
    branches: [main]

env:
  JWT_SECRET: TOPSECRET

jobs:
  Jest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1

      - name: Run Tests
        run: |
          npm install 
          npm run start:dev &
          npm test
